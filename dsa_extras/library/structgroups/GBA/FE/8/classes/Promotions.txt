align:2 count:128 labels:ClassID #offset:0x95dfa4

# Promotion info per class.
# Weirdly, this includes an entry for class ID 0, despite that there is
# no corresponding class struct.
# It also isn't referenced, but just indexed in parallel, presumably.

PROMOTE_TO
    ClassID A
    ClassID B
